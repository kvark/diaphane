# The visualizer is an example, so that its window system stays a
# dev-dependency and never reaches anyone depending on the solver. These spell
# the rest so it stays one word.
[alias]
viz = "run --release --features gui --example viz --"
render = "run --release --example render --"
